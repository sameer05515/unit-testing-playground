# Design Patterns Catalog

A beautiful, interactive catalog of design patterns built with HTML, Alpine.js, and Bootstrap 5.

## Features

- 📋 View all design patterns organized by category
- 🔍 Search patterns by name, intent, or demo
- 🏷️ Filter by category (Creational, Structural, Behavioral)
- 📱 Responsive design for all devices
- 🎨 Modern UI with Bootstrap 5

## Getting Started

### Prerequisites

- Node.js and npm installed

### Installation

1. Install dependencies:
```bash
npm install
```

### Running the Server

Start the HTTP server:

```bash
npm start
```

This will:
- Start the server on port 8080
- Automatically open your browser to `http://localhost:8080`

### Other Commands

- `npm run serve` - Start server without opening browser
- `npm run dev` - Start server with cache disabled (useful for development)

## Project Structure

```
design-patterns/
├── index.html      # Main HTML file
├── patterns.json   # Design patterns data
├── package.json    # Node.js dependencies
└── README.md       # This file
```

## Technologies Used

- **Alpine.js** - Lightweight JavaScript framework
- **Bootstrap 5** - CSS framework
- **http-server** - Simple HTTP server for development

